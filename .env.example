# =============================================================================
# Terminverwaltung - Environment Configuration
# =============================================================================
#
# DO NOT EDIT THIS FILE DIRECTLY!
#
# Run 'make setup' to generate .env with secure random credentials.
# This file is a template only.
#
# =============================================================================

# Database credentials (auto-generated by make setup)
DB_USER="terminverwaltung"
DB_PASSWORD="CHANGE_ME_RUN_MAKE_SETUP"
DB_NAME="terminverwaltung"
DB_HOST="localhost"
DB_PORT="5432"
DATABASE_URL="postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?schema=public"

# App URLs
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_API_URL="http://localhost:3001"

# CORS (comma-separated list of allowed origins for API)
CORS_ORIGIN="http://localhost:3000"

# JWT Authentication (auto-generated by make setup)
JWT_SECRET="CHANGE_ME_RUN_MAKE_SETUP"
JWT_REFRESH_SECRET="CHANGE_ME_RUN_MAKE_SETUP"

# Cron Job Authentication (auto-generated by make setup)
CRON_SECRET="CHANGE_ME_RUN_MAKE_SETUP"

# Email (SMTP) - Optional for development
# In development, emails are captured by Mailpit at http://localhost:8025
# SMTP_HOST=""
# SMTP_PORT=587
# SMTP_USER=""
# SMTP_PASSWORD=""
# SMTP_FROM=""
