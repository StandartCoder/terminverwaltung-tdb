// Re-export constants from shared package
export {
  HTTP_STATUS,
  ERROR_CODES,
  SLOT_DURATION_MINUTES,
  LARGE_COMPANY_THRESHOLD,
} from '@terminverwaltung/shared'

export type { HttpStatus, ErrorCode } from '@terminverwaltung/shared'
