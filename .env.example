# =============================================================================
# Terminverwaltung - Environment Configuration
# =============================================================================
#
# QUICK START:
#   1. Copy this file to .env: cp .env.example .env
#   2. Edit .env with your values
#   3. Run: docker compose up -d
#
# =============================================================================

# -----------------------------------------------------------------------------
# Database (PostgreSQL)
# -----------------------------------------------------------------------------
# For Docker: leave as-is, PostgreSQL runs inside the container
# For external DB: use your connection string
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-this-in-production
POSTGRES_DB=terminverwaltung
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/terminverwaltung?schema=public"

# -----------------------------------------------------------------------------
# Application URLs
# -----------------------------------------------------------------------------
# Change to your actual domain in production
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_API_URL="http://localhost:3001"

# Ports (only change if needed)
PORT_WEB=3000
PORT_API=3001

# -----------------------------------------------------------------------------
# Authentication (NextAuth.js)
# -----------------------------------------------------------------------------
# IMPORTANT: Generate a secure secret for production!
# Run: openssl rand -base64 32
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="generate-a-secure-secret-with-openssl-rand-base64-32"

# -----------------------------------------------------------------------------
# Cron Jobs
# -----------------------------------------------------------------------------
# Secret for authenticating cron requests
# Run: openssl rand -hex 16
CRON_SECRET="generate-a-secure-secret-with-openssl-rand-hex-16"

# -----------------------------------------------------------------------------
# Email (SMTP) - Optional
# -----------------------------------------------------------------------------
# Configure for booking confirmations and reminders
# Leave empty to disable email notifications
SMTP_HOST=""
SMTP_PORT=587
SMTP_USER=""
SMTP_PASSWORD=""
SMTP_FROM="noreply@example.com"

# =============================================================================
# DOCKER DEPLOYMENT
# =============================================================================
#
# Build and run:
#   docker compose up -d --build
#
# View logs:
#   docker compose logs -f
#
# Stop:
#   docker compose down
#
# Reset database (WARNING: deletes all data):
#   docker compose down -v
#   docker compose up -d --build
#
# =============================================================================
